// console.log("arrays!");

const numbers = [1, 2, 3, 4, 5, 6, 7];

console.log(numbers.length);

const people = ["Larry", "Sarah", "Barry", "Susie"];

// pop
// push
// slice
// splice
// join

// sort

console.log(people.sort());